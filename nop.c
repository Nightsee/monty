#include "header.h"
/**
 * fnop - does nothing
 * @stack: pointer
 * @count: line number
 * @G_op: data container
 * Return: nothing
*/
void fnop(stack_t **stack, unsigned int count, operation_t *G_op)
{
	(void) count;
	(void) stack;
	(void) G_op;
}
